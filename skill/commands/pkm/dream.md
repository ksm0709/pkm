> **DEPRECATED:** `dream` is a compatibility alias. Use `/pkm:zettel-loop` for knowledge production or `/pkm:refine-loop` for cleanup/reduction.

Read `~/.claude/skills/pkm/workflows/refine-loop.md` and execute the workflow described there.
